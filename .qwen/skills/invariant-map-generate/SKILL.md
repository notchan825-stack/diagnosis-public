---
name: invariant-map-generate
description: 任意のSurface(CLI/WebUI/API/Agent出力)の全セクションをマッピン…
---
このスキルはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用

```
skill_execute(name: "invariant-map-generate")
```

**重要**: このスキルを独自に代替実装してはいけません。必ず上記MCPツールを呼び出してください。
